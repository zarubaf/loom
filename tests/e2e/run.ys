# SPDX-License-Identifier: Apache-2.0
# End-to-end test: SystemVerilog → read_slang → proc → scan_insert → write_verilog
# Tests the full pipeline with SV-specific features (structs, enums)

read_slang sv_features.sv
hierarchy -check -top sv_features
proc

# Insert scan chain
scan_insert

# Verify we have the expected structure
stat

# Write output for inspection
write_verilog e2e_output.v
